import {
  Platform,
  StyleSheet,
  View,
  Text,
  SafeAreaView,
  ScrollView,
} from "react-native";

export default function HomeScreen() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>
          App
        </Text>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
